

### New Code Changes

```markdown
### Updates

- **API Key Handling:** The API key is now loaded directly from the environment variable `API_KEY`.
- **Model Selection:** The `model` parameter in `client.models.generate_content` has been updated to `gemini-2.0-flash` for generating content.
```